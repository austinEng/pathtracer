
#include <spatial_acceleration/triangle.h>
#include "ray.h"
#include "intersection.h"

template <unsigned int N>
void Intersect(const accel::TriangleGroup<N> &primitive, const Ray &ray, Intersection &intersection) {
    
}